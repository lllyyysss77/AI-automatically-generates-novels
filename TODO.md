# TODO

> 更新于 2026-09-05。已完成项见文末「已落地」。

## P0 · 你明确点过、还没做

### 1. 检索关键词改由大模型生成
现在 `retrieval.py` 用正则 `FACT_TRIGGERS` 抽触发词（知县/流放/交子…），
**这套只对历史题材有效**，金手指、金融商战、电竞、科幻全都泛化不了。

改法：把世界观 / 角色 / 本章细纲丢给 LLM，让它自己回答「写这段需要查证什么」，
产出 3–5 条检索式。题材包的 `research_topics` 降级为给 LLM 的提示，不再是硬清单。
正则保留为 LLM 不可用时的兜底。

### 2. 自带 SearXNG，装完即用
现在假设 `127.0.0.1:18888` 已经跑着，这对新用户是断的。
应内置 `docker-compose.yml`（app + searxng + settings.yml），一条命令起全套；
检索不可用时自动降级为纯内部记忆，不阻塞写作。

---

## P1 · 质量关键（读了 30 章后发现的真问题）

### 3. 角色状态机与时间线
跨章跟踪每个角色的**当前位置 / 状态 / 持有物 / 伤病**，以及**每章发生在第几天**。
现在没有这层，人物会瞬移、伤口会自愈、时间会错乱。
写完每章自动更新，进 L5 硬约束。

### 4. 伏笔自动抽取与回收追踪
`memory.db` 里有 `foreshadow` 表，但**没有任何代码往里写**。
应在每章写完后由 LLM 抽出「本章新埋的伏笔」与「本章回收的伏笔」自动登记；
未回收超过 N 章的进 L5 提醒模型择机回收。

### 5. 卷 / 幕层级真正落地
`packs/type/novel.json` 已声明四层（总纲→分卷→章节细纲→正文），
但编排层 `step_chapter_outlines` **直接从总纲跳到分章**，卷层被跳过。
后果：192 章缺中层节奏控制，主线容易散。

### 6. 章节重写三模式
`replace`（整章重写）/ `polish`（只改语言）/ `fork`（从某章分叉），
旧稿自动备份到 `.ckpt/`，并提示哪些后续章节受影响。

---

## P2 · 功能补全

### 7. UI 补齐新能力入口
后端已有、前端还没接：全书体检、窗口体检、设定返修、历史模式选择、
检索源管理与手动查资料、事实卡查看、快捷词条 shift+L（24 条已抢救但没接线）。

### 8. 迁移老版拆书与思维导图
`book-splitter.js`（1011 行）与 `mind.js`（1291 行）未迁移。
拆书结果应直接落进知识库 / 角色档案 —— 这才算闭环（关掉 issue #5）；
思维导图用于总纲与卷结构可视化。

### 9. 导出补 docx / epub
工作室要 docx 交稿、epub 上架。现在只有 txt / md / 大纲 / fountain / srt。

### 10. Token 成本统计与预算控制
按项目、按阶段记录 token 与耗时。`settings.yaml` 里的 `daily_call_budget`
目前是**空壳，没有任何代码读它**。

---

## P3 · 运营与安全

### 11. 访问鉴权
服务绑 `0.0.0.0` 且**无任何登录**。公网可达时任何人都能进来调模型。
加 cookie 密码鉴权即可（可照搬 x10086 web_ui 的实现）。

### 12. 全新重跑并出质量对比报告
用全部新机制重跑西门庆，与归档的 `_v1_` 稿做 `book_audit` 对比。
基线：v1 全书体检 **29/100**（逐章却有 95.2，说明单章体检不够）。

---

## 已落地

- 四层可插拔：Provider / ContentType / GenrePack / Exporter
- **检索也已 provider 化**（`server/providers/search.py`），与模型网关并列可插拔
- 响应适配器：11 种厂商返回形状，`content/reasoning_content/reasoning/thinking/result/parts`
- 分层记忆 L0–L5 + 预算分配器，32k–100k 自动推导，逐层用量可视化
- 统一召回：内部 FTS5 + 外部检索合并，查过的知识点全书复用
- **全流程背景落地**：世界观 / 人物关系 / 章节剧情三步都先查真实背景
- 题材驱动的检索主题：都市查股市融资、电竞查赛事规则、修真查典籍原型
- 提示词编译器：块状 `#标题` / 人物卡 `[本章未出现]` / 正负词库 / `【字数标记】` / 编号剧情
- 质检三层：单章 → **邻章窗口** → 全书，且结论**回流到下一章的约束**
- 世界观锚定 + 历史模式 `real/alt/none`（写真宋朝就用真名，架空才禁）
- 设定返修：写到一半发现世界观污染，不必推倒重来
- 密钥全部外置 `.env`，仓库零凭据；只保留 `/` 主路由
- Playwright 14/14
