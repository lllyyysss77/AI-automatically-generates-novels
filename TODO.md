# TODO

> 更新于 2026-09-05。P0–P3 已全部完成，剩余为下一轮候选。

## 下一轮候选

### 1. 思维导图（总纲 / 卷结构可视化）
老版 `mind.js`（1291 行）未迁移。拆书已迁移并闭环，导图还没有。
用途：总纲与分卷结构的可视化编辑，比纯文本大纲更适合梳理主线支线。

### 2. 多线并行叙事
现在是单主线推进。长篇需要主线 / 支线交替，配角独立线程。
需要在卷层增加线程标记，并在细纲阶段分配镜头。

### 3. 跨项目风格指纹去重
同一模型写不同的书会复用同一批比喻（实测 v1「铁锥在脑仁里搅动」，
v2「铁锤在脑仁里敲」）。需要跨项目记录已用过的比喻与开场方式。

### 4. 多项目并发生成
现在一次只能跑一本。工作室需要并行多本，且共享检索缓存与事实卡。

### 5. LLM-as-Judge 裁判
现有三层质检都是规则+统计。缺一个换厂模型做主观打分
（开头钩子 / 人设立体 / 节奏 / 爽感），并入回归基准。

---

## 已落地

### 架构
- 四层可插拔：Provider / ContentType / GenrePack / Exporter
- **检索也已 provider 化**（`providers/search.py`），与模型网关并列，配在同一份 yaml
- **自带 SearXNG**：docker-compose + settings.yml + setup.sh，装完即用；不可用自动降级
- 响应适配器：11 种厂商返回形状（content / reasoning_content / reasoning / thinking / result / parts / message 嵌套 / 块数组…）

### 记忆与检索
- 分层记忆 L0–L5 + 预算分配器，32k–100k 自动推导，逐层用量可视化
- 统一召回：内部 FTS5 + 外部检索合并，查过的知识点全书复用
- **检索式由大模型生成**：都市重生场景实测，正则只抓 1 条废话，模型给 5 条精准检索式
- **全流程背景落地**：世界观 / 人物关系 / 章节剧情 / 正文四处都先查真实背景
- 题材驱动的检索主题：都市查股市融资、电竞查赛事规则、修真查典籍原型

### 质量
- **质检三层**：单章 → 邻章窗口 → 全书，三者结论全部回流到下一章约束
- **自我改进循环**：每 5 章自读自批，产出《本书写作守则》注入后续章节
- 套话改词根正则（「瞳孔一缩」字面只中 2 次，变体 9 次全漏）
- **冷启动口癖防护**：22 条默认口癖开局即禁，不等统计攒够 3 章
- 伏笔自动抽取与回收追踪；超 15 章未回收进约束告急
- 角色状态机（所在地/身体状态/持有物）+ 时间线
- 字数配额：剧情条数与目标字数挂钩（实测 10 条剧情写 2600 字会写到 4300 字）

### 世界观治理
- 历史模式 `real / alt / none`：写真宋朝就用真名，只有架空才禁
- 世界观锚定 + 设定返修（写到一半发现污染不必推倒重来）
- 架空世界禁用真实历史人物检测（v1 基线里蔡京出现 43 次）
- 主角别名锚定（对外「西门庆」/ 本名「林远」不混用）

### 创作
- 提示词编译器：`#标题` 块 / 人物卡 `[本章未出现]` / 正负词库 / `【字数标记】` / 编号剧情
- 卷层级落地：写细纲只带所属卷，不再把整本总纲怼进去
- 章节重写三模式 polish / replace / fork，旧稿备份到 `.ckpt/`
- v5.2 资产全保留：130 条题材右键指令 + 24 条快捷词条

### 交付与运营
- 导出：txt / md / 大纲 / **docx / epub** / fountain / srt（docx 与 epub 手写 zip+XML，无新依赖）
- **拆书并落进项目**：世界观 / 角色 / 爽点 / 节奏 / 文风分别入库，闭环 issue #5
- **token 用量统计**：按 profile 分组，写进项目看板；`daily_call_budget` 真正生效
- **访问鉴权**：cookie 密码，`NOVEL_PASSWORD` 留空则不启用

### 工程
- 密钥全部外置 `.env`，仓库零凭据；只保留 `/` 主路由
- 自带 SearXNG（docker-compose），装完即用；不可用自动降级
- Playwright 16/16

---

## 基线数据

| | v1（旧机制） | v2（新机制） |
|---|---|---|
| 全书体检 | **20 / 100** | 待测 |
| 口癖「嘴角勾起」 | 43 次 / 30 章 | 待测 |
| 禁用术语 | 宋律 28、大宋 20 | 待测 |
| 真实历史人物 | 蔡京 43、高俅 3 | 待测 |
| 单章字数均值 | 2529（目标 2600） | 待测 |
